{
  "name": "melektron-ai",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm run build:ton-client",
    "build:ton-client": "cd packages/ton-client && npm run build",
    "build": "npm run build:web",
    "build:web": "cd apps/web && npm run build"
  },
  "devDependencies": {
    "turbo": "^1.10.12"
  }
}