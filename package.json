{
  "name": "melektron-ai",
  "private": true,
  "scripts": {
    "build": "npm run build -w @melektron/quantum-core && npm run build -w @melektron/ton-client && npm run build -w web",
    "dev": "npm run dev -w web",
    "lint": "npm run lint -w web",
    "test": "npm run test -w @melektron/quantum-core && npm run test -w @melektron/ton-client"
  }
}