{
  "name": "melektron-ai",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build --workspace=packages/ton-client && npm run build --workspace=apps/web",
    "postinstall": "cd packages/ton-client && npm install && npm run build"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "typescript": "^5.0.4"
  }
}