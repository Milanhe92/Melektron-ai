name: Vercel Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: |
          npm config set legacy-peer-deps true
          npm install

      - name: Build project
        run: |
          cd packages/ton-client && npm run build
          cd ../../apps/web && npm run build

      - name: Pull Vercel project settings
        run: |
          cd apps/web
          npx vercel pull --yes --token=${{ secrets.VERCEL_TOKEN }} --scope=melektron

      - name: Build with Vercel
        run: |
          cd apps/web
          npx vercel build --token=${{ secrets.VERCEL_TOKEN }} --scope=melektron

      - name: Deploy to Vercel
        run: |
          cd apps/web
          npx vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }} --scope=melektron
        env:
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}