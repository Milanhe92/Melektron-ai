- name: Deploy to Vercel
  uses: amondnet/vercel-action@v25
  with:
    vercel-token: ${{ secrets.VERCEL_TOKEN }}
    # Користи ИД тима за org-id, а scope name за scope
    vercel-org-id: "team_fbUqlATb6X0MiU7YXaDgjvQ4"
    vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
    vercel-args: '--prod'
    scope: "melektron"  # Ово је кључно!