// NOVI, ISPRAVAN KOD
{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**", "!apps/web/.next/cache/**"]
    },
    "lint": {
      "outputs":
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}